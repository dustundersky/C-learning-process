它们的逻辑结构和线性表相同。
特点在于运算受到了限制：
  栈按“后进先出”或“先进后出”的规则进行操作，
  队列按“先进先出”的规则进行操作，
  故称它们为操作受限制的线性表，是一种特定类型的线性表，类似数学学习函数里面我们会学一些经典函数一样。

这样操作有什么好处呢？
栈定义为一种塔的游戏规则，先进后出，

（1）栈初始化：Init_Stack ( s )
表示生成一个空栈，定义这样一个类型
（2）判栈空：Empty_Stack ( s )
这一步有什么意义？就是判断存在的栈有没有数据
（3）入栈：Push_Stack ( s，x )
数据增加操作
（4）出栈：Pop_Stack ( s )
数据删除操作
（5）读栈顶元素：Top_Stack ( s )
数据读取操作

栈最主要的特点就是实现递归。递归就是程序运行中可以调用自己。

队列可以根据上面的内容进行推测。
